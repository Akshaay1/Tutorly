# Runtime dependencies for SERVING the app (used by the deploy host).
# The heavier build-only tools (pypdf, pymupdf, pytesseract, pillow) live in
# rag/requirements.txt and are only needed to (re)build the index locally.
fastapi
uvicorn[standard]
numpy
python-dotenv
google-genai
groq
youtube-transcript-api
yt-dlp
